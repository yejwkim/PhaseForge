# desktop — Student Client (later)

Electron installed desktop app for students. Must be a native installed app (not a browser)
for lockdown (kiosk / fullscreen) and external document-camera access for work-photo capture.

**Out of Phase 1 scope.** This is a later build step (client v1). Placeholder only.
